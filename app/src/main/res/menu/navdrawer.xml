<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <group
        android:checkableBehavior="single"
        android:id="@+id/grp1">
        <item
            android:id="@+id/score"
            android:enabled="true"
            android:title="Score"
            android:icon="@drawable/fm3"
            />
    </group>

    <group android:checkableBehavior="single"
           android:id="@+id/grp2">
        <item
            android:id="@+id/stand"
            android:enabled="true"
            android:title="Team Standings"
            android:icon="@drawable/fm4"
            />
    </group>

    <group android:checkableBehavior="single"
        android:id="@+id/grp3">
        <item
            android:id="@+id/team"
            android:enabled="true"
            android:title="Football Country"
            android:icon="@drawable/fm2"
            />
    </group>

    <group android:checkableBehavior="single"
        android:id="@+id/grp4">
        <item
            android:id="@+id/video"
            android:enabled="true"
            android:title="Video Matches"
            android:icon="@drawable/fm1"
            />
    </group>


</menu>